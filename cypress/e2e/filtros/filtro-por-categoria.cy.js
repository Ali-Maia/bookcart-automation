import HomePage from '../../pageObjects/home/pagina-home';

describe('', () => {
 const home = new HomePage;

 beforeEach(() => {
     home.acessarHome();
 });
  it('', () => {
    // home.acessarHome();
  });

});